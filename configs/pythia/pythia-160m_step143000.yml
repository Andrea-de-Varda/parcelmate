# Pythia training dynamics (LOG.md Iteration 22): EleutherAI/pythia-160m at training step 143000.
# Generated by scripts/make_pythia_configs.py; edit that script, not this file.
#
# The confirmed pipeline (Iteration 21) on ALL 12 x 3072 = 36864 MLP
# neurons: |r| connectome, Fisher-transformed standardized profiles sparsified to their top
# 10%, PCA-100 unwhitened, Lloyd k-means with 200 restarts and Hungarian consensus, k = 100,
# fitted within one domain. Same seed as every earlier run, so every checkpoint sees the
# same documents; only the weights differ along the training axis.
#
# The connectivity step writes the two split halves directly (`outputs: [halves]`) and the
# parcellation consumes only them; `-s purge_connectivity` deletes the 43.5 GB of
# connectivity per tree once metrics/scores.csv exists.
output_dir: results/pythia/pythia-160m/step143000
seed: 42
purge_connectivity: true
connectivity:
  model_name: EleutherAI/pythia-160m
  revision: step143000
  unit_type: mlp
  units_per_layer: null
  n_samples: 4
  domains:
  - wikitext
  - bookcorpus
  - agnews
  - tldr17
  seq_len: 1024
  n_tokens: null
  split: train
  take: 100000
  wrap: true
  shuffle: true
  batch_size: 8
  highpass: null
  lowpass: null
  step: 0.2
  timecourse_pca_components: null
  timecourse_ica_components: null
  eps: 0.001
  null_model: circshift
  n_surrogates: 0
  outputs:
  - halves
parcellation:
  n_networks: 100
  n_samples: 200
  clustering: kmeans
  binarize_connectivity: false
  fisher_transform: true
  standardize_profiles: true
  sparsify_profiles: true
  connectivity_pca_components: 100
  pca_whiten: false
  store_samples: true
  parcellate_keys:
  - halfA
  - halfB
  clustering_kwargs: null
  n_alignments: null
  weight_samples: false
  parcellate_samples: false
parcellation_variants:
  final: {}
